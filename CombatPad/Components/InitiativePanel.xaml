<UserControl x:Class="CombatPad.Components.InitiativePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CombatPad.Components"
             mc:Ignorable="d" 
             d:Background="{StaticResource Application.Brushes.Solid.Parchment}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <local:HeaderControl />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="25" />
                <ColumnDefinition Width="25" />
                <ColumnDefinition Width="25" />
                <ColumnDefinition Width="25" />
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Column="1" Style="{StaticResource Application.Styles.Rectangle.Circle}"/>
            <Rectangle Grid.Column="2" Style="{StaticResource Application.Styles.Rectangle.Skulls}"/>
            <Rectangle Grid.Column="3" Style="{StaticResource Application.Styles.Rectangle.Skulls}"/>
            <Rectangle Grid.Column="4" Style="{StaticResource Application.Styles.Rectangle.Skulls}"/>
            <Rectangle Grid.Column="0" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Left"/>
            <Rectangle Grid.Column="0" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>
            <Rectangle Grid.Column="1" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>
            <Rectangle Grid.Column="2" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>
            <Rectangle Grid.Column="3" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>
            <Rectangle Grid.Column="4" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>
            <Rectangle Grid.Column="5" Stroke="{StaticResource Application.Brushes.Gradient.Red}" HorizontalAlignment="Right"/>

            <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:InitiativePanel}}, Path=ItemsSource}"
                          Grid.ColumnSpan="5"
                          d:ItemsSource="{d:SampleData ItemCount=1}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <local:MyDragCanvas SelectedItem="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:InitiativePanel}}, Path=SelectedItem, Mode=TwoWay}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:DraggableItemComponent Item="{Binding}" Height="35" Width="250"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
